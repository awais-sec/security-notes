# Shell Scripting

Notes on Bash fundamentals, worked through as part of course and bootcamp material and cleaned up here for reference. Content is reorganized into markdown but the underlying material (concepts, examples, practice tasks) is sourced from that coursework rather than self-authored from scratch.

## Contents

| # | File | Covers |
|---|---|---|
| 01 | [common-linux-commands.md](01-common-linux-commands.md) | Core Linux commands for Kali: file/directory management, permissions, process management, networking, package management, system monitoring, user management, file searching |
| 02 | [conditional-execution.md](02-conditional-execution.md) | `if` / `elif` / `else`, file tests, numeric and string comparisons, nested conditionals, logical operators |
| 03 | [wildcard-expansion.md](03-wildcard-expansion.md) | Globbing with `*`, `?`, `[ ]`, `{ }`, `~`, and `**`; wildcards vs. regular expressions |
| 04 | [loops.md](04-loops.md) | `for`, `while`, and `until` loops, loop control (`break`/`continue`), practice tasks including log scanning and disk usage checks |
| 05 | [functions-and-libraries.md](05-functions-and-libraries.md) | Function syntax and parameters, building and sourcing reusable libraries, logging and input-validation patterns |
| 06 | [arrays.md](06-arrays.md) | Declaring, accessing, and modifying arrays, iteration, searching/sorting/slicing, applications like flagging suspicious files and diffing log sets |
| 07 | [process-management.md](07-process-management.md) | `ps`, `top`, `pgrep`, `kill`, job control (`fg`/`bg`), process states, priority with `nice`/`renice`, zombie process detection |
| 08 | [file-and-text-manipulation.md](08-file-and-text-manipulation.md) | `cat`, `grep`, `awk`, `sed`, `cut`, `sort`, `uniq`, `wc`, `tr` across file merging, searching, replacing, and log analysis tasks |

## Notes

- Each file preserves the original code examples and practice tasks as given, with formatting cleaned up for consistency.
